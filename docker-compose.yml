services:
    prolog-api:
        build:
            context: ./backend
            dockerfile: Dockerfile
        # Port not exposed to the host — accessible only within Docker network.
        # Access via http://prolog-api:4000 from the web container.
        expose:
            - "4000"
        healthcheck:
            test: ["CMD", "curl", "-f", "http://localhost:4000/api/health"]
            interval: 10s
            timeout: 5s
            retries: 3
        restart: unless-stopped

    web:
        build:
            context: ./web
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment:
            - NODE_ENV=production
            - PROLOG_API_URL=http://prolog-api:4000
        depends_on:
            prolog-api:
                condition: service_healthy
        restart: unless-stopped
